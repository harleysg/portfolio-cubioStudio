include ./templates/_mixin
<!DOCTYPE html>
html(lang="en")
    block config
        -var mainClass = 'on-index'
        -var data = 'index'
        -var menu = [ {"link": '#about', "text": 'nosotros'}, {"link": '#works', "text": 'trabajos'}, {"link": '#enfoque', "text": 'enfoque'}, {"link": '#contact', "text": 'contacto'}]
        include ./templates/_head
    body
        main(id="app" class="o-main"+' '+mainClass)
            include ./templates/_header
            |
            include ./templates/index/_welcome
            |
            include ./templates/index/_works
            |
            include ./templates/index/_enfoque
            |
            include ./templates/index/_founders
            |
            include ./templates/index/_contact
            |
            |
            include ./templates/_footer
            |
        div(class="mouse-tracker")
        |
        |
        include ./templates/_append
        link(rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/Swiper/4.3.5/css/swiper.min.css")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/Swiper/4.3.5/js/swiper.min.js")
        script.
            document.addEventListener('DOMContentLoaded', () => {
                var swiper = new Swiper('.blog-slider', {
                    spaceBetween: 60,
                    effect: 'fade',
                    loop: true,
                    mousewheel: false,
                    autoplay: {
                        delay: 5000,
                        disableOnInteraction: false,
                    },
                    pagination: {
                        el: '.blog-slider__pagination',
                        clickable: true,
                    }
                });
            });